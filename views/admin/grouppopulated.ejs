<!doctype html>
<html>
<head>
	<meta http-equiv="Content-Type" content="text/html" charset="utf-8" />
	<title>Admin - Group Populated</title>
	<link rel="stylesheet" href="../../css/bootstrap.min.css" title="Bootstrap" type="text/css" media="screen" charset="utf-8">
	<link rel="stylesheet" href="../../css/bootstrap-responsive.min.css" title="Bootstrap responsive" type="text/css" media="screen" charset="utf-8">

	
</head>
<body>
	
	
	<div class="alert alert-success">
		Group Populated
	</div>
	
	<div>
		<p>The secret for the group is <strong><%= secret %></strong> and here are the registration numbers: </p>
		
		<% if ( boyTokens.length > 0 ) { %>
			<h3>Boys</h3>
			<ul>
				<% for (var i = 0; i < boyTokens.length; i++ ){ %>
					<li><strong><%= boyTokens[i] %></strong></li>
				<% } %>
			</ul>
		<% } %>

		<% if ( girlTokens.length > 0 ) { %>		
		<h3>Girls</h3>
		<ul>
			<% for (var i = 0; i < girlTokens.length; i++ ){ %>
				<li><strong><%= girlTokens[i] %></strong></li>
			<% } %>
		</ul>
		<% } %>
		
		
	</div>
	
	
</body>
</html>